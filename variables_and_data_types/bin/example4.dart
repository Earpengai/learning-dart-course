void main(List<String> args) {
  String name = 'Foo'; // declare with strong type
  var address = ''; // declare with var identify base on first declare;
  print(name);
  print(address);

  //address = 123; this will be error
}
